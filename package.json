{
  "name": "t2g-app",
  "version": "1.0.0",
  "private": true,
  "description": "TeeToGreen — native wrapper via Capacitor",
  "scripts": {
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "latest",
    "@capacitor/geolocation": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "@capacitor/ios": "latest"
  }
}
